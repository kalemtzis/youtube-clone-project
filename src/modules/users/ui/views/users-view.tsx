interface Props {
  userId: string;
}

export const UsersView = ({ userId }: Props) => {
  return (
    <div>
      {userId}
      {/* Banner Section */}
      {/* User Info Section */}
      {/* User Videos Section */}
    </div>
  )
}